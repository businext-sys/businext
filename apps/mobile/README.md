# Businext Mobile

Aplicacion movil de Businext (Expo + React Native, Fase 5 del plan maestro,
issues #27-#33 en `businext-sys/businext`). Pendiente de inicializar.

Cuando se cree (issue #27), este paquete se llamara `@businext/mobile` y
declarara `"@businext/shared-core": "workspace:*"` como dependencia, igual que
`apps/web`. Ver `.specify/PLAN.md` seccion 3.4 y `packages/shared-core/README.md`
para el criterio de que codigo va en `shared-core` vs en `mobile`.
